# URL of the API as reachable from the user's browser
NEXT_PUBLIC_API_URL=/api/v1
# URL of the API as reachable from the Next.js server (Docker service name in production)
API_INTERNAL_URL=http://localhost:4000
# Root platform domain used for tenant subdomain resolution
NEXT_PUBLIC_PLATFORM_DOMAIN=pharmasaas.com
